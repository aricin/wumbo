# API DTOs

This folder contains DTOs for the API boundary.

These are lightweight request/response shapes used at the HTTP edge.

They are not domain entities and they are not database schema definitions.

Typical responsibilities:

- request body parsing
- field validation
- small API-facing response shapes when needed
