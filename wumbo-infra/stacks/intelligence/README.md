# intelligence stacks

This workload group is reserved for future analytics, enrichment, and machine
learning infrastructure.

The `dev` and `prod` directories are placeholders so the workload boundary is
visible before those Terraform roots exist.
